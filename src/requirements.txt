# Azure Functions dependencies
azure-functions>=1.18.0

# Build tools for grpc dependencies
wheel>=0.37.0
setuptools>=65.0.0

# gRPC dependencies (with specific versions that have pre-built wheels)
grpcio>=1.50.0,<1.60.0
grpcio-tools>=1.50.0,<1.60.0

# HTTP client for API calls
aiohttp>=3.8.0
requests>=2.28.0

# MCP (Model Context Protocol) dependencies
mcp>=1.0.0

# JSON and data handling
pydantic>=2.0.0

# Data processing 
numpy>=1.24.0

# Environment variables
python-dotenv>=1.0.0